from app.router import router


# test create timer

#creates timer in db storage with correct data

#calls schedule timer function

#returns timer id and time left 

# returns 500 if timer scheduling fails

#handle invalid request ? -

# test get timer

# retunrns correct time left for existing timer

# returns 404 for non existing timer

# returns 0 for expired timer

